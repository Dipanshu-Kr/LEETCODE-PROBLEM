class Solution {
    public boolean isPerfectSquare(int num) {
       boolean isPerfect = false;
       for(long i = 1; i * i <= num; i++){
        if((i * i) == num){
            isPerfect = true;
        }
       } 
       return isPerfect;
    }
}
